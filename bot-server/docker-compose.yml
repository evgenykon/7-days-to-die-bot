services:
  bot-server:
    build: .
    ports:
      - "9091:9091"
    environment:
      - BOT_URL=http://host.docker.internal:9090
      - PORT=9091
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
